import React from 'react';
import PropTypes from 'prop-types';

const Header = () => {
    return (
        <h1 className="text-center bg-success text-white py-2">Contact Keeper</h1>
    );
};

Header.propTypes = {};

export default Header;
